"""AI provider and prompt-execution boundary."""

# TODO: integrate Gemini, execute prompt_versions, inject RAG context, persist results, and redact PII.
# The Phase 2 endpoint deliberately uses a deterministic mock response.
