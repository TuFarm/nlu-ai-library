import { useState } from "react";
import { books, categories } from "../../services/mockApi";
export default function KioskBookSuggestionScreen({ onBack, onSurvey }: { onBack: () => void; onSurvey: () => void }) { const [category,setCategory]=useState(categories[0]); return <div className="kiosk-books"><div className="kiosk-section-heading"><div><span className="kiosk-kicker">GỢI Ý ĐƠN GIẢN THEO CHỦ ĐỀ</span><h1>Khám phá cuốn sách tiếp theo</h1></div><button className="kiosk-ghost" onClick={onBack}>← Trở lại trò chuyện</button></div><div className="kiosk-category-list">{categories.map(c=><button className={c===category?"active":""} onClick={()=>setCategory(c)} key={c}>{c}</button>)}</div><div className="kiosk-book-grid">{books.filter(b=>b.category===category).map(b=><article key={b.title}><div className="kiosk-book-cover">NLU<small>LIBRARY</small></div><span>{b.category}</span><h2>{b.title}</h2><p>{b.author_name}</p><small>Mã hệ thống thư viện: {b.external_book_id}</small></article>)}</div><div className="kiosk-bottom-actions"><p>Đây là gợi ý mô phỏng, không phải danh mục sách đầy đủ.</p><button onClick={onSurvey}>Hoàn tất & đánh giá →</button></div></div> }
