import { FormEvent, useState } from "react";
import { apiClient } from "../../services/apiClient";
export default function KioskSurveyScreen({ onComplete }: { onComplete: () => void }) { const [rating,setRating]=useState(0); const [helped,setHelped]=useState(""); async function submit(e:FormEvent){e.preventDefault();try{await apiClient.post("/surveys/survey-2026-01/responses/mock",{answers:{rating,helped}})}catch{/* Mock mode intentionally continues offline. */}onComplete()} return <form className="kiosk-survey" onSubmit={submit}><span className="kiosk-kicker">CHỈ MẤT KHOẢNG 1 PHÚT</span><h1>Trải nghiệm hôm nay của bạn thế nào?</h1><p>Phản hồi của bạn giúp trợ lý thư viện tốt hơn mỗi ngày.</p><fieldset><legend>Bạn có hài lòng với câu trả lời của AI không?</legend><div className="kiosk-rating">{[1,2,3,4,5].map(n=><button type="button" className={n<=rating?"selected":""} onClick={()=>setRating(n)} key={n}>★<small>{n}</small></button>)}</div></fieldset><fieldset><legend>AI có giúp bạn giảm thời gian hỏi lễ tân không?</legend><div className="kiosk-choices">{["Có","Không","Chưa chắc"].map(v=><button type="button" className={helped===v?"selected":""} onClick={()=>setHelped(v)} key={v}>{v}</button>)}</div></fieldset><button className="kiosk-primary" disabled={!rating||!helped}>Gửi phản hồi</button></form> }
