import type { ReactNode } from "react";
export function KioskChrome({ children, state, onExit }: { children: ReactNode; state: string; onExit?: () => void }) { return <section className="kiosk-screen"><header className="kiosk-top"><div className="kiosk-brand"><span>NL</span><div><strong>NLU Library</strong><small>Trợ lý lễ tân AI</small></div></div><div className="kiosk-status"><i/> {state.replaceAll("_", " ")}</div>{onExit && <button className="kiosk-exit" onClick={onExit}>Kết thúc phiên</button>}</header><div className="kiosk-content">{children}</div><footer>Phiên bản thử nghiệm · Không lưu ảnh khuôn mặt</footer></section> }
