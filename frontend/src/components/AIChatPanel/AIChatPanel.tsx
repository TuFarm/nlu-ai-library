export default function AIChatPanel() {
  return (
    <main data-screen="ai-chat">
      <h1>Hỏi trợ lý thư viện</h1>
      <p>Câu trả lời trong tương lai sẽ được tạo từ tài liệu đã xử lý qua RAG.</p>
      {/* TODO: persist conversation messages and render grounded AI responses. */}
    </main>
  );
}
